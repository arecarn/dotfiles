---
- name: MacOSX
  tags: python
  when: ansible_distribution == 'MacOSX'
  with_items: "{{packages}}"
  homebrew:
    name: "{{item}}"
    state: latest

- name: Ubuntu
  tags: python
  become: yes
  when: ansible_distribution == 'Ubuntu'
  with_items: "{{packages}}"
  package:
    name: "{{item}}"
    state: latest

---
- name: Install common packages
  ansible.builtin.package:
    name: "{{ common_packages }}"
    state: present

- name: Install OS-family-specific packages (RHEL/Ubuntu)
  ansible.builtin.package:
    name: "{{ os_family_packages[ansible_facts['os_family']] }}"
    state: present

---
- name: Source shell profile from .profile
  become: false
  ansible.builtin.lineinfile:
    dest: "{{ user_home }}/.profile"
    insertafter: EOF
    line: ". ~/.config/shell/profile"
    create: true
    mode: "0644"
